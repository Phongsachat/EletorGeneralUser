import 'dart:ui';

import 'package:flutter/material.dart';

///Login Button
const btnLoginButton = darkColor;
const btnGoogleSignIn = Color(0xFFF1E6FF);

///Login TextField
const bgTxtField = Color(0xFFF1E6FF);
const iconTxtField = Colors.black;
const cursorTxtFieldColor = Colors.black;
const iconTxtFieldColor = Colors.black;
const hintTxtFieldColor = Colors.grey;

///General
const btnAccept = Colors.green;
const btnCancel = Colors.grey;

const darkColor = Color(0xFF475520);
const primaryColor = Color(0xFF68783C);
const secondColor = Color(0xFFB7D36A);

/// Application Background
const bgPrimary = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [darkColor, primaryColor, secondColor]);

const primaryFontFamily = "prompt";

//ReportList
const sendReportColor = Color(0xFF00A2FF);
const onProcessColor = Color(0xFFF5D400);
const missionCompleteColor = Color(0xFF2DA15F);

///Menu Bar
const iconMenu1 = Icon(
  Icons.home_outlined,
  color: Colors.black,
  size: 30,
);
const iconMenu2 = Icon(
  Icons.campaign,
  color: Colors.black,
  size: 30,
);
const iconMenu3 = Icon(
  Icons.history,
  color: Colors.black,
  size: 30,
);
